const db = require("../db");

class Order {
    static async (listOrdersForUser) {
        // return all orders authenticated user has created
    }

    static async (createOrder) {
        //take user's order and store in database
    }
}